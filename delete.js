let array=[12,23,6,44,96,09]
let place=parseInt(prompt("Enter Index where you wants to delete an element"))
switch (place) {
    case place:
        console.log("Before deletion "+array);
        array.splice(place,1)
        console.log("After deletion "+array);
        break;

    default:
        break;
}